# require Rails.root.join("app/models/user.rb")
# require Rails.root.join("app/controllers/devise/sessions_controller.rb")

# those codes above were added to deal with the issue with sessions_controler
# however deleting those codes fixed the issue in docker container of which web container did not work