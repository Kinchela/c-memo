<% provide(:title, "Measurement") %>
<h1>身体測定</h1>

<%= render 'child', child: @child %>

<h3>身長</3>
<% if @height_data.present? %>
  <%= line_chart @height_data, library: { title: { text: 'Height' } } %>
<% else %>
  <p>No data available</p>
<% end %>
<h3>体重</h3>
<% if @height_data.present? %>
  <%= line_chart @weight_data, library: { title: { text: 'Weight' } } %>
<% else %>
  <p>No data available</p>
<% end %>